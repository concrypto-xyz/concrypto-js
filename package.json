{
	"name": "concrypto-js",
	"version": "0.0.2",
	"description": "API to interacting with ConCrypto services",
	"main": "dist/index.js",
	"module": "dist/index.mjs",
	"types": "dist/index.d.ts",
	"files": [
		"dist/**/*.js",
		"dist/**/*.d.ts",
		"dist/**/*.mjs"
	],
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsup src/index.ts --format cjs,esm --dts",
		"prepublish": "npm run build",
		"publish": "npm publish --access public"
	},
	"keywords": [],
	"author": "Manuel Rivera",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/pay-with-crypto/pay-with-crypto-js"
	},
	"license": "Apache-2.0",
	"devDependencies": {
		"tsup": "^8.3.5",
		"typescript": "^5.6.3"
	},
	"dependencies": {
		"axios": "^1.7.7"
	}
}
